export default function validateSeriesForm(form){

    let errors = {}
  
    const afterEditingDescanso=()=>{
        /*var text= form["tiempoEntreSeries"]
        if(/:/.test(text)){
          // formatTiempoDescanso(text)
    
        }else if(text!=undefined && text.length>0){
          
          text=text.length>3 ? insertIntoString(text,2,":") : insertIntoString(text,1,":")
          // formatTiempoDescanso(text)
          setForm({...form,["tiempoEntreSeries"]:""+text})
        }else{
          setForm({...form,["tiempoEntreSeries"]:"00:00"})
    
        }*/
        
      }
    
    return errors
}